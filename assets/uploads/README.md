# Imágenes cargadas

Esta carpeta puede usarse para imágenes añadidas manualmente al catálogo estático. Las imágenes subidas desde `/admin` se guardan en la carpeta persistente del backend y se publican mediante `/media/`.
